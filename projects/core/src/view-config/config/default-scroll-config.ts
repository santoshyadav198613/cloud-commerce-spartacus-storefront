import { ViewConfig } from './view-config';

export const defaultScrollConfig: ViewConfig = {
  view: {
    infiniteScroll: {
      active: true,
      productLimit: 0,
      showMoreButton: false,
    },
  },
};
